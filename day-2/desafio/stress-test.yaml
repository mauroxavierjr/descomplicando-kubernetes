apiVersion: v1
kind: Pod
metadata:
  name: stress-test
  labels:
    app: stress
spec:
  containers:
  - name: ubuntu
    image: ubuntu:latest
    args:
    - sleep
    - infinity
    resources:
      limits:
        memory: "256Mi"
        cpu: "0.5"
      requests:
        memory: "128Mi"
        cpu: "0.3"
